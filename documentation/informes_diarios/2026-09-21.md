# Informe diario STER — 21/09/2026

Informe parcial. Se registran resultados comprobados, no cierre del día.

## Cambios

- Módulo de filtros implementado en vertical: sistemas, subsistemas, filtros,
  registro, historial y administración, con los componentes visuales existentes.
- Persistencia local aditiva, identidad de tablet, UUID, numeración ODT compartida
  y sincronización mediante uploader seguro USB.
- Catálogo inicial validado de 7 sistemas, 31 subsistemas y 46 registros de filtros.
  El nuevo MDB_SYSTEM aclara CODE_SYS 3 = Combustible diésel, 4 = Agua.
- Carga inicial protegida: no pisa catálogos ni pendientes existentes y no repone
  filtros borrados después de descargar desde el servidor, incluso si queda vacío.
- Herramienta de diagnóstico de BD de solo lectura, con credenciales locales.
- Preparación de paquete de uploader mediante lista explícita de archivos,
  excluyendo contraseñas, bases de tablets y registros privados.

## Verificaciones y entrega

- Análisis Flutter sin incidencias; suite completa: 291 pruebas aprobadas.
- Python: 104 pruebas aprobadas de filtros, uploader, origen y limpieza de plato.
- APK debug ARM64 compilado e instalado con actualización en TABR70000000012091.
- Copia de seguridad previa e integridad SQLite comprobada.
- Comparación antes/después: 27 tablas y 697 registros anteriores sin cambios.
- Confirmada carga en tablet: 7 sistemas, 31 subsistemas y 46 filtros.
- App iniciada tras instalar. No se crearon mantenimientos ficticios en producción.

## Pendientes y límites

- La BD responde en red, pero requiere contraseña en el uploader seguro.
  No se validaron todavía sus índices, permisos ni escrituras reales.
- Descargar catálogo real y ejecutar una prueba controlada con dos tablets.
- Actualizar todos los uploaders que asignan ODT para que compartan el bloqueo.
- FLT_CHANGE no incluye cantidad histórica ni identificación del elemento retirado.
- Sin cambios de esquema remoto, sin commit ni push.
- El catálogo de motores incluido no se refrescó desde el servidor en esta compilación.

## Evidencias

### Corrección visual e inicio

- Adaptación de las ocho vistas al diseño aprobado: ilustraciones vectoriales,
  tarjetas turquesa, barra lateral y disposición vertical.
- Tarjeta «Cambio de filtros» en el Home, visible antes de las herramientas,
  con acceso a `/filtros`. Más espacio para los contadores del inicio.
- Cambios de presentación; no se modificaron guardado, sincronización ni BD remota.
- Análisis final sin incidencias y suite Flutter completa: 293 pruebas aprobadas.
- APK actualizado e instalado en TABR70000000012091, sin desinstalar ni borrar datos.
- Verificación visual real del Home y acceso al catálogo desde su nueva tarjeta.
- Comparación tras la actualización visual y Home: 33 tablas y 782 registros
  conservados sin diferencias; integridad SQLite `ok`.
- Capturas: `output/design/filtros/qa/after-home-tablet.png` y
  `output/design/filtros/qa/after-systems-tablet.png`.
- Un build falló al copiar el APK por bloqueo temporal de Windows; el reintento
  terminó correctamente. Graphify actualizado sin purgar archivos excluidos.

### Evidencias de la implementación inicial

### Ajuste de posición e icono solicitado después

- «Cambio de filtros» se movió debajo de los otros módulos del Home y antes
  de Recientes, manteniendo su acceso directo.
- La barra izquierda usa el mismo cartucho vectorial que la tarjeta, con un
  único componente compartido y conservando su etiqueta «Cambio de filtros».
- Análisis limpio; nueve pruebas de Home/Filtros aprobadas (incluyen orden y
  navegación por ambos accesos). APK compilado e instalado con actualización.
- Respaldo previo: `output/tablet-backups/20260921-filters-order-before/`.
- Evidencia: `output/design/filtros/qa/home-filters-below-tablet.png`.
- Graphify actualizado; el JSON de catálogo no produce nodos AST y se mantienen
  los nodos anteriores de archivos excluidos, sin forzar su eliminación.

### Archivos de referencia

- `documentation/MODULO_FILTROS_IMPLEMENTACION.md`
- `documentation/FILTROS_PRUEBA_TABLET.md`
- `test/filter_seed_test.dart`, `test_filter_sync.py`
- `output/tablet-backups/20260921-131537-TABR70000000012091/`
- `build/app/outputs/flutter-apk/app-debug.apk`
