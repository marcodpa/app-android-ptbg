# Informe diario de cambios — STER

Fecha: **15 de septiembre de 2026**.
Corte de actividades: **17:00**, America/Caracas (UTC−04:00).
Actualización documental: **15/09/2026 a las 17:04**.
Estado: **cerrado al corte**, según las evidencias disponibles en esta tarea.

## Resumen

Se atendió la solicitud «sube todo al repo»: se publicó el estado actual de la
app y su APK en la rama `main` de `marcodpa/app-android-ptbg`, conservando el
historial. El commit `baf13724dabd57b0fea75c5a8d5122ddd70ff103`, creado hoy a las
08:30:49, contiene 221 archivos nuevos, modificados o retirados. La subida
terminó correctamente y se comprobó que el identificador remoto coincide.

Pasaron **118 pruebas Python y 6 pruebas Flutter: 124 en total**, ejecutadas
hoy en la copia preparada para publicar. No se compiló ni se instaló una nueva
versión hoy. El APK publicado corresponde a la entrega **1.1.1+3 del 10/09**.
La publicación no acredita validación en producción.

## Cambios por módulo y estado

### Repositorio y distribución — solicitado y realizado hoy

- Se preparó una copia del repositorio remoto porque el directorio de trabajo
  no tenía repositorio Git propio y heredaba el de la carpeta del usuario.
  Se conservó el historial y se publicó sin forzar ni reescribir la rama.
- Se ajustaron `.gitignore` y `.gitattributes` para excluir respaldos, datos
  locales, credenciales locales, capturas y temporales, y preservar los
  archivos binarios. Se mantuvo el APK bajo Git LFS.
- Se retiraron del repositorio siete imágenes antiguas y
  `lib/providers/app_provider.dart`, ya ausentes de la app local actual y sin
  referencias activas verificadas. Siguen recuperables en el historial.
- Git LFS confirmó la transferencia completa del APK de **177 415 256 bytes**;
  el envío de `main` terminó correctamente.
- Se dejó el directorio del proyecto conectado a su propio repositorio Git.
  Permanecen dos CSV generados de `qr_salida/` con diferencias locales, no
  publicados en este envío; no se borraron ni sobrescribieron.

### App, servicios, catálogo, formatos e identidad de tablet

**Sin cambios funcionales nuevos verificados hoy.** Se publicaron los trabajos
acumulados: servicios y pantallas, formatos, catálogo, separadores tipo 10,
limpieza de plato, uploader seguro e identificación de tablet por ODT.
El tamaño del commit no representa desarrollo realizado íntegramente hoy.
Las fechas confirmadas de las entregas de septiembre permanecen en los
[informes del 08/09](2026-09-08.md), [09/09](2026-09-09.md) y
[10/09](2026-09-10.md); no se asigna una fecha de implementación a otros cambios
solo porque aparezcan en este commit.

### Documentación

Se incluyeron en la publicación los informes existentes del 08/09 al 11/09.
Este cierre del 15/09 y la actualización de su índice se guardan localmente
después del corte; **no forman parte del commit publicado por la mañana**.
Esta ejecución del informe no realiza un nuevo commit ni una nueva subida.

## Pruebas y comprobaciones

| Verificación realizada hoy antes de publicar | Resultado confirmado |
| --- | --- |
| Python: `test_tablet_origin`, `test_tablet_uploader`, `test_separator_cleaning`, `test_reporte_texto`, `test_report_zero_values`, `test_compressor_checklist_report`, `test_actualizar_catalogo_apk` | 118 pruebas aprobadas; 9,439 s |
| Flutter: `test/usb_only_sync_test.dart` y `test/alignment_sync_contract_test.dart` | 6 pruebas aprobadas; `All tests passed!` |
| Revisión de rutas y patrones sensibles antes de publicar | Sin coincidencias en los patrones de tokens y claves privadas revisados; excluidos archivos locales sensibles. No equivale a una auditoría completa de seguridad |
| Publicación de Git y Git LFS | Envío correcto; `main` remoto coincide con el commit local |

No se volvió a ejecutar hoy la suite completa de 275 pruebas Flutter ni el
análisis completo documentados el 10/09. Las 118 pruebas Python sí se repitieron
hoy; no se suman sus ejecuciones anteriores al total diario.

## APK, instalaciones y producción

- Artefacto publicado: `build/app/outputs/flutter-apk/app-debug.apk`.
- SHA-256: `EF1D1EEEA64EAB566BCE1212B45161892D21F56FFD799DED792F8D3E985AAA68`.
- El hash local comprobado durante el cierre coincide con el puntero LFS del
  commit y con la entrega documentada el 10/09.
- **Compilado hoy: no. Instalado hoy: no. Validado en producción hoy: no.**
- Las instalaciones confirmadas del 10/09 a las 13:43:56 y 13:58:38 son
  antecedentes, no entregas nuevas de esta jornada. No se consultó la tablet
  durante este cierre ni se comprobó su estado actual.

## Pendientes y riesgos

1. Validar una ODT nueva y su `TABLET_ORIGEN` en MariaDB mediante el uploader
   seguro, y repetir con una segunda tablet. No consta confirmación de estas
   pruebas reales; las pruebas locales no acreditan concurrencia en planta.
2. Recibir el formulario oficial exclusivo de separadores tipo 10 y validar
   el ciclo real de limpieza de plato.
3. Mantener separados los registros antiguos sin origen de las nuevas ODT.
   Identificar la tablet no resuelve por sí solo todos los conflictos de sincronización.
4. Mantener los límites de la revisión de seguridad: la clave de aplicación
   preexistente en `Api_scv_ptbg.py` no se modificó en esta publicación. No se
   reproduce su valor ni se certifica una auditoría de todo el historial público.
5. El APK distribuido es debug y depende de Git LFS para recuperar el binario
   completo. La advertencia previa de compatibilidad futura Kotlin no se
   abordó ni se volvió a evaluar mediante una compilación hoy.

## Arrastres y límites temporales

**Sin cambios verificados** posteriores al corte del 14/09 a las 17:00 y
anteriores a la actividad del 15/09 que deban atribuirse a otra fecha.
No hay actividad técnica adicional comprobable del 12/09 al 14/09 en la
conversación revisada; esto no afirma ausencia de trabajo externo a esta tarea.
El informe del 11/09 permanece como registro parcial previo, sin sobrescribirlo
ni inventar cierres de fechas intermedias.

La elaboración de este documento ocurre después de las 17:00 y es trabajo
documental del 15/09. Durante este cierre solo se revisaron evidencias, se
consultó la referencia remota y se guardaron documentos y memoria de consulta:
no se modificó código, no se ejecutaron pruebas nuevamente, no se instaló APK
y no se sincronizaron ni consultaron bases de datos.

## Evidencias

- [Commit publicado](https://github.com/marcodpa/app-android-ptbg/commit/baf13724dabd57b0fea75c5a8d5122ddd70ff103): alcance, archivos y fecha del commit.
- Conversación de esta tarea del 15/09: solicitud de publicación; resultados
  Python y Flutter; transferencia LFS al 100 %; confirmación de envío
  `1a411ec..baf1372 main -> main` y cotejo local/remoto.
- Cierre actual: consulta del historial, estado local, puntero LFS, hash del APK
  y referencia remota `refs/heads/main`, todavía en `baf13724...`.
- [Entrega del 10/09](2026-09-10.md) y
  [guía de prueba de TABLET_ORIGEN](../PRUEBA_TABLET_ORIGEN.md).
- Graphify orientó la consulta mediante `report`, `commit`, `apk` y `test`;
  ubicó referencias de pruebas y uploader, pero no acredita fechas ni resultados.

